# Aplicar UI-04

1. Copia el contenido del parche sobre la raíz de la versión UI-03.1.
2. Conserva tus archivos `.env` y la carpeta `.data`.
3. Ejecuta:

```bash
npm run web:install
npm run web:build
npm run dev
```

En otra terminal:

```bash
npm run web:dev
```

Abre `http://localhost:5173`.
