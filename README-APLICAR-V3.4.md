# Aplicación V3.4

1. Sustituye los archivos del parche.
2. Ejecuta `npm run web:install`.
3. Arranca backend con `npm run dev`.
4. En otra terminal ejecuta `npm run web:dev`.
5. Abre http://localhost:5173.
