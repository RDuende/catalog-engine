# Aplicar M4.2

Copiar el contenido del parche sobre la raíz del proyecto M4.1, conservando las rutas.

Después ejecutar:

```bash
npm install
npm run test:m4-2
```

Endpoint SSE nuevo:

`GET /api/v1/tasks/:taskId/stream`
