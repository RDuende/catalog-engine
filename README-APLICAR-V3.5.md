# Aplicar V3.5

1. Copia el contenido del parche sobre V3.4.
2. Ejecuta `npm run web:install`.
3. Arranca el backend con `npm run dev`.
4. Arranca la web con `npm run web:dev`.
5. Abre `http://localhost:5173`.
