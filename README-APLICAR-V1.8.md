# Aplicar V1.8

Copiar el contenido del parche sobre la raíz de V1.7 y permitir la sustitución de `package.json`.

Validar con:

```bash
npm install
npm run typecheck
npm run test:v1-8
npm run demo:v1-8
```
