import type { KnowledgeEntityDefinition } from "./knowledge-entity.types.js";
export const normalizeKnowledgeText=(v:string)=>v.normalize("NFD").replace(/[\u0300-\u036f]/gu,"").toLowerCase().replace(/\s+/gu," ").trim();
export const tokenizeKnowledgeText=(v:string):readonly string[]=>Object.freeze(normalizeKnowledgeText(v).split(/[^a-z0-9]+/u).filter(Boolean));
const contains=(source:string,term:string)=>{const s=normalizeKnowledgeText(source),t=normalizeKnowledgeText(term);if(!t)return false;const tt=tokenizeKnowledgeText(t);return tt.length===1?new Set(tokenizeKnowledgeText(s)).has(tt[0]??""):s.includes(t);};
export function evaluateKnowledgeRule(source:string,d:KnowledgeEntityDefinition){const direct=[d.id,d.displayName.es,d.displayName.en,...d.aliases].filter(t=>contains(source,t));const strong=(d.strongTerms??[]).filter(t=>contains(source,t));const reqAny=(d.requiresAny??[]).filter(t=>contains(source,t));const reqAll=(d.requiresAll??[]).filter(t=>contains(source,t));const excluded=(d.excludesAny??[]).filter(t=>contains(source,t));return Object.freeze({allowed:(direct.length>0||strong.length>0)&&(!d.requiresAny?.length||reqAny.length>0)&&(!d.requiresAll?.length||reqAll.length===d.requiresAll.length)&&excluded.length===0,matchedDirect:Object.freeze(direct),matchedStrong:Object.freeze(strong),matchedRequired:Object.freeze([...reqAny,...reqAll]),matchedExcluded:Object.freeze(excluded)});}
