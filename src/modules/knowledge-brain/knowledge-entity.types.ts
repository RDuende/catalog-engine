export type KnowledgeEntityKind = "INTEREST" | "MATERIAL" | "FEATURE" | "PROFESSION" | "THEME" | "TECHNIQUE" | "OCCASION" | "RECIPIENT" | "OBJECT" | "BRAND" | "LICENSE" | "AMBIGUOUS";
export type KnowledgeEvidenceSource = "DIRECT" | "STRONG" | "CONTEXT_RULE" | "INTEREST_BRAIN" | "PRODUCT_BRAIN" | "EXISTING_DATA";
export interface KnowledgeEntityDefinition { readonly id:string; readonly kind:KnowledgeEntityKind; readonly domain:string; readonly displayName:{readonly es:string;readonly en:string}; readonly aliases:readonly string[]; readonly strongTerms?:readonly string[]; readonly requiresAny?:readonly string[]; readonly requiresAll?:readonly string[]; readonly excludesAny?:readonly string[]; }
export interface KnowledgeEvidence { readonly entityId:string; readonly kind:KnowledgeEntityKind; readonly confidence:number; readonly source:KnowledgeEvidenceSource; readonly matchedTerms:readonly string[]; readonly rule?:string; }
export interface KnowledgeProfile { readonly interests:readonly string[]; readonly materials:readonly string[]; readonly features:readonly string[]; readonly professions:readonly string[]; readonly themes:readonly string[]; readonly techniques:readonly string[]; readonly occasions:readonly string[]; readonly recipients:readonly string[]; readonly objects:readonly string[]; readonly brands:readonly string[]; readonly licenses:readonly string[]; readonly evidence:readonly KnowledgeEvidence[]; readonly version:"1.0"; }
export interface KnowledgeAnalyzeInput { readonly text:string; readonly existing?:Partial<Pick<KnowledgeProfile,"interests"|"materials"|"features"|"professions"|"themes"|"techniques"|"occasions"|"recipients"|"objects"|"brands"|"licenses">>; }
