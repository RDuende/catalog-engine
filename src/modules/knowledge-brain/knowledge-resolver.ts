import { evaluateKnowledgeRule } from "./knowledge-context-analyzer.js";
import type { KnowledgeEntityDefinition,KnowledgeEvidence } from "./knowledge-entity.types.js";
export function resolveKnowledgeEntity(source:string,d:KnowledgeEntityDefinition):KnowledgeEvidence|undefined{const r=evaluateKnowledgeRule(source,d);if(!r.allowed)return undefined;const direct=r.matchedDirect.length>0,strong=r.matchedStrong.length>0,ctx=r.matchedRequired.length>0;const confidence=Math.min(1,(direct?.82:strong?.76:0)+Math.min(.12,r.matchedStrong.length*.04)+(ctx?.06:0));return Object.freeze({entityId:d.id,kind:d.kind,confidence:Math.round(confidence*1000)/1000,source:ctx?"CONTEXT_RULE":direct?"DIRECT":"STRONG",matchedTerms:Object.freeze([...new Set([...r.matchedDirect,...r.matchedStrong,...r.matchedRequired])]),...(ctx?{rule:"requires-context"}:{})});}
