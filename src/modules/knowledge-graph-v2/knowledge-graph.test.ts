import assert from "node:assert/strict";import test from "node:test";import {KnowledgeGraphService} from "./knowledge-graph.service.js";import type {KnowledgeGraphRepository,KnowledgeEntity} from "./knowledge-graph.types.js";
class MemoryRepo implements KnowledgeGraphRepository{entities:KnowledgeEntity[]=[];async createEntity(i:any){const e={...i,id:String(this.entities.length+1),slug:i.slug??i.name.toLowerCase(),description:i.description??null,metadata:i.metadata??{},createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()};this.entities.push(e);return e}async listEntities(f:any){return this.entities.filter(e=>!f.type||e.type===f.type).slice(f.offset,f.offset+f.limit)}async getEntity(id:string){return this.entities.find(e=>e.id===id)??null}async createRelation(i:any){return i}async getProductGraph(){return null}async stats(){return{entities:this.entities.length,relations:0,productLinks:0,aliases:0,byType:{}}}}
test("crea y lista entidades tipadas",async()=>{const s=new KnowledgeGraphService(new MemoryRepo());await s.createEntity({type:"MATERIAL",key:"bamboo",name:"Bambú"});const rows=await s.listEntities({type:"MATERIAL"});assert.equal(rows.length,1);assert.equal(rows[0]?.name,"Bambú")});
test("rechaza relaciones reflexivas",async()=>{const s=new KnowledgeGraphService(new MemoryRepo());await assert.rejects(()=>s.createRelation({sourceId:"1",targetId:"1",type:"RELATED_TO"}),/consigo misma/)});
