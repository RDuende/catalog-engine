# Aplicar M3.1.2

Copia el contenido del parche sobre la raíz de `catalog-engine`, permitiendo sobrescribir los dos tests indicados.

Después ejecuta:

```bash
npm run typecheck
npm run test:rai-runtime
npm run test:m3-1
```
