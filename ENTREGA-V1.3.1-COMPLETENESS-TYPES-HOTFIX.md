# V1.3.1 — Completeness Types Hotfix

Corrige:

- Uso correcto de `DiscoveryExtractor` antes de `applyDiscovery`.
- `applyDiscovery` devuelve directamente `JourneyProject`.
- Tipado explícito de `journeyTypes` como `readonly JourneyType[]`.
- Alineación del requisito de presupuesto con el hecho real `budget.max`.
