# Aplicar RC2

1. Sustituye los archivos del parche en la raíz de `catalog-engine`.
2. Ejecuta `npm run web:install`.
3. Ejecuta `npm run test:rc2`.
4. Arranca backend con `npm run dev` y frontend con `npm run web:dev`.
