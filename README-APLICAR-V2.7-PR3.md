# Aplicar V2.7 PR-3

Copiar el contenido del parche sobre la raíz de V2.7 PR-2 y ejecutar:

```bash
npm install
npm run typecheck
npm run test:v2-7-pr3
```

Configura opcionalmente:

```env
ARTIFACT_STORAGE_DIR=.data/artifacts
```
